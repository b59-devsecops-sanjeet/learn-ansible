- name: Demo on fileVars
  hosts: all
  vars:
    url: "play.google.com"
  vars_files:
    - "{{env}}-vars.yml"
  tasks:
    - name: Printing variables from file
      ansible.builtin.debug:
        msg: "Current Environment is  {{env}} and the team name is {{DEV-LEAD}} the value is {{A}}"